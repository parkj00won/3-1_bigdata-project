# MoodMate: 텍스트 기반 감정 분석 및 맞춤 추천 서비스

- **이름 / 학번**: 박주원 / 20232500
- **데이터 출처 / 라이선스**: HuggingFace `dair-ai/emotion` ([https://huggingface.co/datasets/dair-ai/emotion](https://huggingface.co/datasets/dair-ai/emotion)) / MIT License

---

## 1. 문제 정의

현대인은 바쁜 일상 속에서 자신의 감정 상태를 명확히 인식하지 못하는 경우가 많다. 일기나 메모처럼 짧은 한 문장으로 오늘의 기분을 표현했을 때, 그 감정이 무엇인지 자동으로 분류하고 그에 맞는 활동이나 콘텐츠를 추천해 준다면 심리적 자기 인식을 높이는 데 도움이 될 수 있다. 본 프로젝트는 영어·한국어 감정 텍스트 데이터를 학습하여 사용자의 짧은 문장에서 감정을 예측하고, 감정별 맞춤 활동을 추천하는 웹 서비스를 구축하는 것을 목표로 한다.

- **예측 대상(target)**: 입력 문장의 감정 클래스 6종 — `sadness(슬픔)`, `joy(기쁨)`, `love(사랑)`, `anger(분노)`, `fear(두려움)`, `surprise(놀라움)`

---

## 2. 데이터 소개 & EDA

### 데이터 규모 및 형식

| 항목 | 내용 |
|------|------|
| 데이터셋 | `dair-ai/emotion` (HuggingFace Datasets) |
| train / validation / test | 16,000 / 2,000 / 2,000 행 |
| 컬럼 | `text` (영어 단문), `label` (감정 정수 0~5) |
| 클래스 수 | 6종 (sadness, joy, love, anger, fear, surprise) |
| 로컬 로그 | `emotion_log.csv` — datetime, text, emotion, emotion_korean, confidence, recommended_action |

### EDA에서 발견한 사실

1. **클래스 불균형**: `joy`와 `sadness`가 전체 데이터의 과반을 차지하며, `love`와 `surprise`는 상대적으로 적다. 이는 모델이 소수 클래스를 덜 정확하게 예측할 수 있는 위험 요인이다.

2. **감정별 문장 길이 차이**: `love`·`joy` 계열 문장이 `anger`·`fear`보다 평균 단어 수가 많다. 감정 표현의 풍부함과 어휘 다양성이 감정 유형마다 다름을 의미한다.

3. **특정 어휘의 감정 집중**: TF-IDF 가중치 상위 단어를 보면 `feel`, `like`, `im`, `love` 등 감정 동사·형용사가 전체 분류에서 높은 기여도를 보이며, 각 감정별로 대표 어휘가 뚜렷하게 구분된다.

### 결측·이상치 처리

- HuggingFace `datasets`로 로드 시 `text`·`label` 컬럼에 결측값 없음 (공식 데이터셋).
- 빈 문자열 또는 공백만 있는 입력은 앱 레벨에서 사전 차단 (입력 유효성 검사).
- 수치형 이상치 없음 — 텍스트 길이 분포를 확인한 결과 극단적으로 긴 문장이 없어 원본 데이터를 유지하였다.

---

## 3. 특성 엔지니어링

| 새 특성 | 생성 방법 | 사용 근거 |
|---------|-----------|-----------|
| `text_length` | 문장의 문자 수 계산 | 감정별 표현 길이 차이를 확인하기 위해 사용 |
| `word_count` | 공백 기준 토큰 수 계산 | 감정별 평균 단어 수 비교 및 EDA 시각화에 활용 |
| `emotion` | 숫자 라벨 → 감정명 문자열 변환 | 시각화·결과 해석을 쉽게 하기 위해 사용 |
| **한국어 키워드 매핑** | 유니코드 범위(가–힣)로 한국어 감지 → 감정별 키워드 사전(6종 × 9~15개)으로 매칭 → 표준 영어 문장으로 변환 | 영어 전용 모델로 한국어 입력을 처리하기 위한 하이브리드 설계 |

한국어 키워드 매핑은 데이터셋이 영어 기반임에도 실사용 환경을 고려한 설계로, 별도 다국어 모델 없이 언어 장벽을 극복하기 위해 도입하였다.

---

## 4. 모델 / 서비스

### 사용한 방법: **전통 머신러닝 (TF-IDF + Logistic Regression)**

딥러닝·LLM 대신 전통 ML을 선택한 이유:
- 6-클래스 단문 감정 분류에서 Logistic Regression은 해석 가능성이 높고 학습·추론 속도가 빠르다.
- GPU 없이 로컬 환경에서 안정적으로 동작하며 `pkl` 파일로 모델을 저장·재사용할 수 있다.
- 수업 프로젝트의 목적이 복잡한 모델보다 데이터 분석 → 모델링 → 서비스화의 전체 흐름 구현에 있으므로, 가볍고 설명 가능한 모델을 선택하였다.

### 입력 → 출력 흐름

```
사용자 입력 문장
    │
    ├─ (한국어 감지) ──▶ 키워드 사전 매핑 ──▶ 영어 문장 변환
    │
    └─ (영어) ──────────────────────────────▶ 그대로 전달
                                              │
                                     TF-IDF 벡터화 (max_features=5,000)
                                              │
                                     Logistic Regression 분류
                                              │
                             감정 레이블 + 신뢰도(%) 출력
                                              │
                                     감정별 맞춤 추천 (활동·콘텐츠)
                                              │
                                     emotion_log.csv 저장
```

### 성능 지표

| 평가 지표 | 값 |
|-----------|-----|
| Accuracy | 0.87 |
| Macro avg F1-score | 0.81 |
| Weighted avg F1-score | 0.87 |

| 감정 | Precision | Recall | F1-score | Support |
|------|-----------|--------|----------|---------|
| sadness | 0.91 | 0.92 | 0.91 | 581 |
| joy | 0.85 | 0.95 | 0.90 | 695 |
| love | 0.80 | 0.65 | 0.72 | 159 |
| anger | 0.89 | 0.83 | 0.86 | 275 |
| fear | 0.87 | 0.82 | 0.84 | 224 |
| surprise | 0.89 | 0.50 | 0.64 | 66 |


---

## 5. Streamlit 앱

### 페이지 구성 (4개 탭)

| 탭 | 주요 내용 |
|----|----------|
| **서비스 소개** | 프로젝트 배경, 데이터셋 설명, 서비스 흐름 안내 |
| **데이터 분석** | 데이터셋 미리보기, 감정 분포 차트, 감정별 평균 단어 수, 예시 문장 |
| **감정 예측** | 텍스트 입력 → 감정 예측 결과 카드 + 확률 막대 그래프 + 맞춤 추천 |
| **감정 기록** | 누적 예측 이력 테이블, 분포 차트, 최근 5건 요약, CSV 내보내기 |

### 실행 방법

```bash
pip install -r requirements.txt
streamlit run app.py
```

> 모델 파일(`model/emotion_model.pkl`, `model/tfidf_vectorizer.pkl`)이 없는 경우 먼저 실행:
> ```bash
> python model_train.py
> ```

---

## 6. 결론 & 한계

### 알게 된 것

- 짧은 감정 문장 분류에서 TF-IDF + Logistic Regression 조합은 높은 신뢰도를 보여, 복잡한 딥러닝 없이도 실용적인 성능을 달성할 수 있음을 확인하였다.
- 한국어 입력을 키워드 사전으로 영어 문장에 매핑하는 하이브리드 접근법이 다국어 지원의 현실적인 대안임을 알게 되었다.
- 데이터 분석·모델링 결과를 실제 사용자 흐름(추천·기록)에 연결하는 것이 서비스 완성도에 중요하다는 점을 배웠다.

### 아쉬운 점 / 향후 과제

1. **한국어 지원 고도화**: 현재 키워드 사전 기반 매핑은 사전에 없는 표현을 처리하지 못한다. 한국어 전용 감정 데이터셋(예: AIHub 감성 대화 말뭉치)으로 별도 모델을 학습하거나 KoBERT·KLUE-BERT 같은 사전학습 모델을 적용하면 정확도를 크게 높일 수 있다.
2. **클래스 불균형 해소**: SMOTE 또는 `class_weight='balanced'` 옵션을 적용해 소수 클래스(`love`, `surprise`)의 예측 성능을 개선할 여지가 있다.
3. **LLM 활용**: Claude나 GPT 같은 사전학습 LLM을 API로 연동하면 맥락을 고려한 더 정교한 감정 분석과 자연스러운 추천 문구 생성이 가능하다.
4. **개인화 추천**: 사용자의 누적 감정 기록을 분석해 자주 나타나는 감정 패턴에 따라 개인화된 콘텐츠를 우선 추천하는 방향으로 확장할 수 있다.
5. **감정 트렌드 시각화**: 시계열 감정 변화 그래프를 추가하면 사용자가 장기적인 감정 흐름을 추적하는 데 도움이 된다.
